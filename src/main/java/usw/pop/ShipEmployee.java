package usw.pop;

import java.math.BigDecimal;

public class ShipEmployee extends Employee {
    public ShipEmployee(String employeeName, String address, BigDecimal salary, int phoneNum) {
        super(employeeName, address, salary, phoneNum);
    }
}
