package usw.pop;

import java.math.BigDecimal;

public class ShipCaptain extends Employee {
    public ShipCaptain(String employeeName, String address, BigDecimal salary, int phoneNum) {
        super(employeeName, address, salary, phoneNum);
    }
}
